- gather_facts: false
  hosts: routers
  tasks:
  - ios_config:
      lines:
      - interface 10004
      - ip verify unicast source reachable-via rx
      - rate-limit input 1000000 187500 375000 conform-action transmit exceed-action
        drop
    name: Mitigar ataque en interface 10004 de S1.cisco.local (192.168.99.2)
  - ios_config:
      lines:
      - interface 10009
      - ip verify unicast source reachable-via rx
      - rate-limit input 1000000 187500 375000 conform-action transmit exceed-action
        drop
    name: Mitigar ataque en interface 10009 de S1.cisco.local (192.168.99.2)
